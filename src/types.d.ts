export type QuoteObject = {
  anime: string;
  character: string;
  quote: string;
};

type Datas = {
  datas: QuoteObject;
};

type PicturesObject = { name: string; numberOfPicturesAvailable: number };
